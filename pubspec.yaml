name: medmanage
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.18.0
  http: ^1.3.0
  cloud_firestore: ^5.6.5
  firebase_core: ^3.12.1 

  english_words: ^4.0.0
  provider: ^6.1.2
  google_fonts: ^6.2.1

  flutter_local_notifications: ^16.3.2 # Use the latest version from pub.dev
  timezone: ^0.9.0 
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
   uses-material-design: true